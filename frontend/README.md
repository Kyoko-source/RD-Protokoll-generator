# NANA Frontend

React/Vite-Grundgeruest fuer die spaetere installierbare NANA-App.

## Start

```powershell
npm install
npm run dev
```

Standard-API: `http://localhost:8000`

Bei Bedarf:

```powershell
$env:VITE_API_BASE="http://localhost:8000"
npm run dev
```
